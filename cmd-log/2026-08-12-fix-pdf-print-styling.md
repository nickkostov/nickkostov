# PDF print styling fix

- Updated `style.css` print rules to retain the terminal theme, typography, borders, and spacing.
- Updated architecture and local-container guidance with the browser Background graphics requirement.
- Verified with `rtk npm run test:smoke` (44 passed), JavaScript syntax, JSON validation, and `git diff --check`.
