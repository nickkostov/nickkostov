# Use the alternative navigation

The horizontal navigation below the terminal title provides a non-terminal route to every main CV section. Select a button with a mouse, or use Tab and Enter/Space from the keyboard.

The buttons run the same commands as terminal input, so both navigation methods display identical content. The selected section is highlighted. Navigation remains disabled while CV content loads or if content validation fails.

On a narrow screen, scroll the navigation horizontally to reach additional sections.
